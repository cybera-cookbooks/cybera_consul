# The primary network interface
auto eth0
iface eth0 inet dhcp
dns-search node.consul
dns-search <%= node[:cybera_consul][:resolv][:search].join(' ') %>
dns-nameservers <%= node[:cybera_consul][:resolv][:nameservers].join(' ') %>
